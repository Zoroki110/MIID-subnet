# Starlette version bounded by FastAPI (<0.38.0, >=0.37.2)
starlette>=0.37.2,<0.38.0
pydantic==2.7.1
rich==13.7.1
pytest==8.2.2
torch==2.3.0
pandas==2.2.2
tqdm==4.66.4
numpy==1.26.4
# NOTE: setuptools is pulled in indirectly; letting pip resolve an appropriate version avoids conflicts with bittensor which requires >=70.
faker==24.8.0
ollama==0.1.9
python-Levenshtein==0.24.0
jellyfish==1.0.4
# aiohttp is pulled transitively by bittensor (3.9/3.10 range), no direct pin needed.
substrate-interface>=1.7.9
bittensor==7.4.0
# NOTE: `bittensor-cli` depends on NumPy 2.x which conflicts with our NumPy 1.26 pin
# required by `bittensor 7.4.0`. If you need the CLI, install it *separately*
# in its own virtual environment (`pip install bittensor-cli --no-deps`).
wandb==0.16.6
python-dotenv==1.0.1